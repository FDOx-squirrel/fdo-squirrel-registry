# Python >= 3.10

rdflib>=7.0            # graph building and canonical serialisation (S4)
pyshacl>=0.26          # SHACL gate (S5)
requests>=2.31         # Zenodo REST API (S2, the only step that uses the network)
PyYAML>=6.0            # queries.yaml (S7)
Jinja2>=3.1            # page templates (S6, S7)
